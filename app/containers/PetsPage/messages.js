/*
 * PetsPage Messages
 *
 * This contains all the text for the PetsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PetsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PetsPage container!',
  },
});
